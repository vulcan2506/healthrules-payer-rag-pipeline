# ── Secrets — never commit these ────────────────────────────────────────────
Stage 1/reanker.txt
Stage 1/groq.txt
.claude/
Stage 1/.claude/
*.env
.env

# ── Not part of this push (unrelated content) ───────────────────────────────
Healthcare Doc/
Healthcare Doc.zip

# ── Python / venv ────────────────────────────────────────────────────────────
venv/
__pycache__/
*.pyc
.deepeval/

# ── Large / regenerable pipeline data (see KT doc, "How To Run It") ─────────
# Source PDFs are the actual licensed product documentation — not ours to
# publish — and everything else here is fully regenerable by running
# main.py -> run_tail.py per the KT document.
# chroma_db/ and index/ are tracked via Git LFS instead of excluded (see
# .gitattributes) — pushed deliberately, per explicit instruction.
# The pdfs/ and output/ directories (and a placeholder README in each)
# stay visible so the repo structure is understandable — everything
# actually inside them (PDFs, generated artifacts) stays excluded.
Stage 1/data/*
!Stage 1/data/pdfs/
!Stage 1/data/output/
Stage 1/data/pdfs/*
!Stage 1/data/pdfs/README.md
Stage 1/data/output/*
!Stage 1/data/output/README.md

# Eval/deep-eval reports & logs quote real retrieved release-note excerpts
# verbatim (proprietary source content) and are regenerated by
# eval.py --compare / deep_eval.py per the KT document — not source.
retrieval_layer/eval_report.md*
retrieval_layer/deep_eval*.md
retrieval_layer/deep_eval*.log

# ── Personal/working files not needed in the repo ───────────────────────────
Stage 1/*.log
Stage 1/WhatsApp Image*.jpeg

# output/

Everything the pipeline generates (chunks, taxonomy, delta reports,
hierarchy/topic summaries, eval reports, etc.) lands here. Gitignored —
fully regenerable by running `main.py` → `run_tail.py` over your own PDFs
in `../pdfs/`, per `KT_Session_Document.docx`. Otherwise empty in the
public repo.
